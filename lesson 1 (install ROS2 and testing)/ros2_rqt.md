# 🐢 Туториал по использованию rqt в ROS 2

## 🔍 Что такое rqt?
rqt — это графический фреймворк для ROS 2, предоставляющий набор плагинов для визуализации и взаимодействия с нодами, топиками, сервисами и параметрами.

---

## 🛠️ Подготовка среды
Перед началом убедитесь, что у вас запущен ROS 2 и активна среда (source установлен).

---

## 📥 Шаг 1: Установка rqt

Откройте новый терминал и выполните команды:

```bash
sudo apt update
sudo apt install '~nros-humble-rqt*'
```

*Примечание: Замените `humble` на вашу версию ROS 2 при необходимости.*

---

## 🚀 Шаг 2: Запуск rqt

Запустите rqt командой:

```bash
rqt
```

![Изображение: Пустое окно rqt при первом запуске](images/rqt.png)

При первом запуске окно будет пустым — это нормально.

**⚠️ Важно:** Если плагины не загрузились:
- Закройте rqt
- Запустите с принудительным обнаружением плагинов:
  ```bash
  rqt --force-discover
  ```

---

## 🎮 Шаг 3: Запуск turtlesim для демонстрации

В разных терминалах запустите:

```bash
# Терминал 1: Запуск turtlesim
ros2 run turtlesim turtlesim_node

# Терминал 2: Запуск управления клавиатурой
ros2 run turtlesim turtle_teleop_key
```

---

## 🛎️ Шаг 4: Использование Service Caller

1. В окне rqt перейдите: **Plugins → Services → Service Caller**
2. Нажмите кнопку **Refresh** (слева от выпадающего списка Service)
3. В списке сервисов выберите `/spawn`

---

## 🐢➡️🐢 Шаг 5: Создание второй черепахи

1. В сервисе `/spawn` задайте параметры:
   - `name`: `'turtle2'` (в двойных кавычках)
   - `x`: `1.0`
   - `y`: `1.0`

![Изображение: Настройка сервиса /spawn](images/spawn.png)

2. Нажмите кнопку **Call**

*Результат:* В окне turtlesim появится вторая черепаха в координатах (1, 1).

**❌ Ошибка:** Если черепаха с таким именем уже существует, в терминале turtlesim_node появится сообщение об ошибке.

---

## 🖌️ Шаг 6: Изменение пера первой черепахи

1. В том же плагине Service Caller выберите сервис `/turtle1/set_pen`
2. Установите параметры:
   - `r`: `255` (красный компонент, 0-255)
   - `g`: `0`
   - `b`: `0`
   - `width`: `5` (толщина линии)

![Изображение: Настройка сервиса /set_pen](images/set_pen.png)

3. Нажмите **Call**

*Результат:* Первая черепаха теперь рисует красной линией толщиной 5 пикселей.

![Изображение: Черепаха с новым пером](images/new_pen.png)

---

## 🎯 Шаг 7: Управление второй черепахой

Для управления второй черепахой нужен отдельный teleop-узел с ремаппингом топика:

```bash
ros2 run turtlesim turtle_teleop_key --ros-args --remap turtle1/cmd_vel:=turtle2/cmd_vel
```

Теперь:
- Первый терминал с teleop управляет `turtle1`
- Второй терминал (с ремаппингом) управляет `turtle2`

![Изображение: Две черепахи с разными управлениями](images/remap.png)

---

## 📊 Шаг 8: Другие полезные плагины rqt

Explore → **Другие полезные плагины**:
- 📡 **Topic Monitor**: Мониторинг топиков и сообщений
- 🕸️ **Node Graph**: Визуализация графа нод
- 📈 **Plot**: Построение графиков данных топиков
- 🎒 **Bag**: Запись и воспроизведение rosbag-файлов

---

## 🛑 Завершение работы

1. В терминале `turtlesim_node`: `Ctrl+C`
2. В терминалах `turtle_teleop_key`: Нажмите `q`

---

## ⚠️ Возможные проблемы и решения

1. **❓ Плагины не отображаются**
   ```bash
   rqt --force-discover
   ```

2. **❓ Сервисы не появляются в списке**
   - Убедитесь, что нода (turtlesim_node) запущена
   - Нажмите Refresh в Service Caller
   - Проверьте, что среда ROS 2 активирована

3. **❓ Ошибка при вызове сервиса**
   - Проверьте тип и формат параметров
   - Убедитесь, что нода активна и не "зависла"

---

## 💪 Практическое задание

1. Создайте третью черепаху с именем `turtle3`
2. Измените цвет пера второй черепахи на синий
3. С помощью **Topic Monitor** понаблюдайте за топиком `/turtle2/pose`

---

## 🎓 Заключение

rqt — мощный инструмент для отладки и взаимодействия с системой ROS 2. Освоив базовые плагины, вы сможете эффективно визуализировать данные, вызывать сервисы и управлять параметрами ваших нод.

*💡 Совет:* Изучите другие плагины rqt через меню Plugins для более полного использования возможностей фреймворка.