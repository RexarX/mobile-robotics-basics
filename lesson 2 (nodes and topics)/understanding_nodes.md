# **Туториал по ROS 2: Понимание узлов (Nodes)**

## 📖 **Что такое узлы (Nodes) в ROS 2?**

### **1. Граф ROS 2**
В следующих туториалах вы познакомитесь с основными концепциями ROS 2, которые составляют **"Граф ROS 2"**.

**Граф ROS 2** — это сеть элементов ROS, которые обрабатывают данные одновременно. Он включает все исполняемые файлы и соединения между ними, если их отобразить визуально.

### **2. Узлы в ROS 2**
Каждый узел в ROS должен выполнять **единственную, модульную задачу**, например:
- Управление двигателями колёс
- Публикация данных с датчика расстояния
- Обработка изображений с камеры

![Узлы и их взаимодействие](images/Nodes-TopicandService.gif)
*Узлы обмениваются данными через топики и сервисы*

Каждый узел может:
- Отправлять данные другим узлам через **топики**
- Предоставлять **сервисы** (запрос-ответ)
- Использовать **действия (actions)** для длительных операций
- Работать с **параметрами**

**Полная роботизированная система** состоит из множества узлов, работающих согласованно. В ROS 2 один исполняемый файл (программа на C++, Python и т.д.) может содержать **один или несколько узлов**.

---

## ✅ **Предварительные требования**
1. Установленный пакет `turtlesim` (из предыдущего туториала)
2. Не забывайте выполнять `source` в каждом новом терминале!

---

## 🚀 **Практическая часть**

### **1. Запуск узлов с помощью `ros2 run`**
Команда `ros2 run` запускает исполняемый файл из пакета:

```bash
ros2 run <имя_пакета> <имя_исполняемого_файла>
```

**Запустим turtlesim:**
```bash
ros2 run turtlesim turtlesim_node
```
Откроется окно turtlesim (как в предыдущем туториале).

Здесь:
- **Имя пакета:** `turtlesim`
- **Имя исполняемого файла:** `turtlesim_node`

Но мы ещё не знаем **имя узла**. Узнать его можно с помощью `ros2 node list`.

### **2. Список всех активных узлов**
```bash
ros2 node list
```

Эта команда показывает имена всех запущенных узлов. Особенно полезна, когда:
- Нужно взаимодействовать с конкретным узлом
- Система запускает много узлов и нужно отслеживать их состояние

**Пример работы:**
1. Запустите turtlesim в первом терминале
2. В новом терминале выполните:
   ```bash
   ros2 node list
   ```
   Результат: `/turtlesim`

3. Запустите телеоперацию в третьем терминале:
   ```bash
   ros2 run turtlesim turtle_teleop_key
   ```

4. Снова выполните `ros2 node list`:
   ```bash
   /turtlesim
   /teleop_turtle
   ```

Теперь видно два активных узла!

#### **2.1 Переназначение (Remapping)**
Remapping позволяет изменять свойства узлов по умолчанию:
- Имя узла
- Имена топиков
- Имена сервисов
- и другие параметры

**Пример переназначения имени узла:**
```bash
ros2 run turtlesim turtlesim_node --ros-args --remap __node:=my_turtle
```

Теперь, если выполнить `ros2 node list`, увидим:
```bash
/my_turtle
/turtlesim
/teleop_turtle
```

Откроется второе окно turtlesim с новым именем узла!

### **3. Полная информация об узле**
Узнав имена узлов, можно получить детальную информацию о каждом:

```bash
ros2 node info <имя_узла>
```

**Пример для нашего нового узла:**
```bash
ros2 node info /my_turtle
```

Вывод покажет:
```
/my_turtle
  Subscribers:
    /parameter_events: rcl_interfaces/msg/ParameterEvent
    /turtle1/cmd_vel: geometry_msgs/msg/Twist
  Publishers:
    /parameter_events: rcl_interfaces/msg/ParameterEvent
    /rosout: rcl_interfaces/msg/Log
    /turtle1/color_sensor: turtlesim/msg/Color
    /turtle1/pose: turtlesim/msg/Pose
  Service Servers:
    /clear: std_srvs/srv/Empty
    /kill: turtlesim/srv/Kill
    ...
  Action Servers:
    /turtle1/rotate_absolute: turtlesim/action/RotateAbsolute
  Action Clients:
```

**Что показывает `ros2 node info`:**
- **Subscribers (Подписчики):** топики, на которые подписан узел
- **Publishers (Издатели):** топики, в которые публикует узел
- **Service Servers (Серверы сервисов):** сервисы, которые предоставляет узел
- **Service Clients (Клиенты сервисов):** сервисы, которые использует узел
- **Action Servers (Серверы действий):** действия, которые предоставляет узел
- **Action Clients (Клиенты действий):** действия, которые использует узел

**Сравните информацию для разных узлов:**
```bash
ros2 node info /teleop_turtle
ros2 node info /turtlesim
```

Обратите внимание, как различаются их подключения в графе ROS!

---

## 📝 **Ключевые выводы**
✅ **Узел** — фундаментальный элемент ROS 2, выполняющий одну модульную задачу  
✅ **Один исполняемый файл** может содержать несколько узлов  
✅ **`ros2 node list`** показывает все активные узлы в системе  
✅ **`ros2 node info`** даёт полную информацию о подключениях узла  
✅ **Remapping** позволяет переназначать свойства узлов при запуске  

---

## 🔜 **Что дальше?**
Теперь, когда вы понимаете узлы в ROS 2, переходите к изучению **[топиков](ссылка_на_туториал_по_топикам)** — одного из основных способов связи между узлами.

---

## 🎮 **Практическое задание**
1. Запустите три разных узла turtlesim с разными именами
2. Используйте `ros2 node list` для проверки всех запущенных узлов
3. Сравните информацию о каждом узле с помощью `ros2 node info`
4. Попробуйте переназначить не только имя узла, но и топик cmd_vel

**Пример переназначения топика:**
```bash
ros2 run turtlesim turtle_teleop_key --ros-args --remap turtle1/cmd_vel:=turtle2/cmd_vel
```